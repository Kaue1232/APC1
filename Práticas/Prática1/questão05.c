/* Faça um programa em C que calcule as raízes de uma equação do 2° grau através da fórmula de Bhaskara (-b +/- raiz_quadrada(b² - 4ac)/2a). */

#include <stdio.h>
int main () { 

  float a;
  float b;
  float c;
  

 printf("Calcule o valor de delta");
float delta= b*b - 4*a*c;
  
  printf("Calcule o valor de x1");
  float x1= (-b + raiz_quadrada_do_valor_de_delta) / (2*a);

  printf("Calcule o valor de x2");
  float x2= (-b - raiz_quadrada_do_valor_de_delta) / (2*a);
  
  

  return 0;
  
  
}